# Framework principal web y renderizado de plantillas (Jinja2)
Flask>=3.0.0

# ORM para la gestión de la base de datos (SQLite)
Flask-SQLAlchemy>=3.1.1

# Gestión de sesiones de usuario y autenticación local
Flask-Login>=0.6.3

# (Opcional pero recomendado) Carga de variables de entorno desde un archivo .env
python-dotenv>=1.0.0

# ---------------------------------------------------------
# DEPENDENCIAS PARA AUTENTICACIÓN CON GOOGLE (OAuth 2.0)
# ---------------------------------------------------------

# Librería integral para construir clientes OAuth (opción 1)
Authlib>=1.3.0

# Librerías oficiales de Google y peticiones HTTP (opción 2)
google-auth>=2.23.0
google-auth-oauthlib>=1.0.0
requests>=2.31.0